# D-kit で読み取れる情報の詳細

このドキュメントでは、D-kitがデザインキットとして保存・読み取れる情報を詳しく説明します。

## 📦 読み取れる情報の全体像

D-kitは以下の4種類の情報を読み取ります：

1. **カラースタイル** (Color Styles)
2. **テキストスタイル** (Text Styles)
3. **エフェクトスタイル** (Effect Styles)
4. **コンポーネント情報** (Components) - 参考情報のみ

---

## 🎨 1. カラースタイル（Color Styles）

### 読み取れる情報

| 項目 | 内容 | 例 |
|------|------|-----|
| **name** | スタイル名 | "Primary/500", "Background/Light" |
| **id** | 一意のID | "S:abc123..." |
| **paints** | 塗りの情報 | 単色、グラデーション、画像など |
| **description** | 説明文 | "メインブランドカラー" |

### paints に含まれる詳細

- **ソリッドカラー（単色）**
  - RGB値
  - 不透明度

- **グラデーション**
  - 線形グラデーション
  - 放射状グラデーション
  - 角度グラデーション
  - グラデーションの色停止点

- **画像塗り**
  - 画像のスケールモード
  - 画像の回転

### 適用時の動作

デザインキット適用時、以下のように色をマッチングします：

1. 既存レイヤーの色を取得
2. RGB値のユークリッド距離を計算
3. 最も近いカラースタイルを適用

**例:**
```
既存の色: #FF5733 (赤系)
↓
デザインキットから最も近い色を探す
↓
適用される色: "Primary/500" (#FF6347)
```

---

## 📝 2. テキストスタイル（Text Styles）

### 読み取れる情報（全9項目）

| 項目 | 内容 | 例 |
|------|------|-----|
| **name** | スタイル名 | "Heading/H1", "Body/Regular" |
| **id** | 一意のID | "S:def456..." |
| **fontSize** | フォントサイズ | 24, 16, 14 など |
| **fontFamily** | フォントファミリー | "Inter", "Noto Sans JP" |
| **fontStyle** | フォントスタイル | "Regular", "Bold", "Italic" |
| **fontWeight** | フォントの太さ | 400, 700, 900 など |
| **lineHeight** | 行の高さ | 1.5, 24px, AUTO など |
| **letterSpacing** | 文字間隔 | 0%, 0.5%, -2% など |
| **textCase** | 大文字/小文字 | ORIGINAL, UPPER, LOWER, TITLE |
| **textDecoration** | 装飾 | NONE, UNDERLINE, STRIKETHROUGH |
| **description** | 説明文 | "見出し用テキスト" |

### 詳細説明

#### fontSize（フォントサイズ）
- 数値で保存（ピクセル単位）
- 例: `16`, `24`, `32`

#### lineHeight（行の高さ）
- 以下の形式をサポート：
  - **数値**: `24` (固定値)
  - **パーセント**: `{ value: 150, unit: "PERCENT" }` (150%)
  - **AUTO**: `{ unit: "AUTO" }`

#### letterSpacing（文字間隔）
- 以下の形式をサポート：
  - **パーセント**: `{ value: 5, unit: "PERCENT" }` (5%)
  - **ピクセル**: `{ value: 2, unit: "PIXELS" }` (2px)

#### textCase（大文字/小文字）
- `ORIGINAL`: そのまま
- `UPPER`: すべて大文字
- `LOWER`: すべて小文字
- `TITLE`: タイトルケース（各単語の頭文字が大文字）

#### textDecoration（装飾）
- `NONE`: なし
- `UNDERLINE`: 下線
- `STRIKETHROUGH`: 打ち消し線

### 適用時の動作

デザインキット適用時、以下のようにテキストスタイルをマッチングします：

1. 既存テキストの `fontSize` を取得
2. デザインキット内で最も近いフォントサイズのスタイルを探す
3. そのスタイルの全プロパティ（フォント、行間、文字間隔など）を適用

**例:**
```
既存テキスト: fontSize = 18px
↓
デザインキットから最も近いサイズを探す
↓
適用されるスタイル: "Body/Large" (fontSize = 16px)
→ フォント、行間、文字間隔などもすべて適用
```

**⚠️ 重要な注意点:**
- フォントサイズのみで判断（フォントファミリーは考慮しない）
- フォントが存在しない場合、代替フォントで適用を試みる

---

## ✨ 3. エフェクトスタイル（Effect Styles）

### 読み取れる情報

| 項目 | 内容 | 例 |
|------|------|-----|
| **name** | スタイル名 | "Shadow/Drop", "Blur/Background" |
| **id** | 一意のID | "S:ghi789..." |
| **effects** | エフェクトの配列 | ドロップシャドウ、ブラーなど |
| **description** | 説明文 | "カード用の影" |

### effects に含まれる詳細

#### ドロップシャドウ（DROP_SHADOW）
- X/Y オフセット
- ブラー半径
- スプレッド
- 色と不透明度

#### インナーシャドウ（INNER_SHADOW）
- ドロップシャドウと同じパラメータ
- 内側に適用

#### レイヤーブラー（LAYER_BLUR）
- ブラー半径

#### 背景ブラー（BACKGROUND_BLUR）
- ブラー半径

### 適用時の動作

エフェクトスタイルは、レイヤーに既存のエフェクトがある場合のみ適用されます。

**例:**
```
既存レイヤー: ドロップシャドウあり
↓
デザインキットのエフェクトスタイルを適用
↓
適用結果: デザインキットの "Shadow/Drop" が適用される
```

---

## 🧩 4. コンポーネント情報（Components）

### 読み取れる情報

| 項目 | 内容 | 例 |
|------|------|-----|
| **name** | コンポーネント名 | "Button/Primary", "Card/Default" |
| **id** | 一意のID | "123:456" |
| **description** | 説明文 | "プライマリボタン" |
| **type** | タイプ | "COMPONENT" |

### 制限事項

- **制限なし** - すべてのコンポーネントを保存
- **自動置き換え機能あり** - 同名のコンポーネントを自動的に置き換え
- バリアント情報も保持

---

## 📊 実際の保存データ例

### 最大限に読み取った場合の例

```json
{
  "name": "Material Design Kit",
  "sourceFile": "Material Design Kit",
  "exportedAt": "2025/01/26 10:30:00",
  "colors": [
    {
      "name": "Primary/500",
      "id": "S:abc123",
      "paints": [{ "type": "SOLID", "color": { "r": 1, "g": 0.39, "b": 0.28 }, "opacity": 1 }],
      "description": "メインブランドカラー"
    },
    {
      "name": "Background/Light",
      "id": "S:def456",
      "paints": [{ "type": "SOLID", "color": { "r": 0.98, "g": 0.98, "b": 0.98 }, "opacity": 1 }],
      "description": ""
    }
    // ... その他のカラースタイル
  ],
  "textStyles": [
    {
      "name": "Heading/H1",
      "id": "S:text001",
      "fontSize": 32,
      "fontFamily": "Inter",
      "fontStyle": "Bold",
      "fontWeight": 700,
      "lineHeight": { "value": 140, "unit": "PERCENT" },
      "letterSpacing": { "value": -1, "unit": "PERCENT" },
      "textCase": "ORIGINAL",
      "textDecoration": "NONE",
      "description": "大見出し用"
    },
    {
      "name": "Body/Regular",
      "id": "S:text002",
      "fontSize": 16,
      "fontFamily": "Inter",
      "fontStyle": "Regular",
      "fontWeight": 400,
      "lineHeight": { "value": 150, "unit": "PERCENT" },
      "letterSpacing": { "value": 0, "unit": "PERCENT" },
      "textCase": "ORIGINAL",
      "textDecoration": "NONE",
      "description": "本文用"
    }
    // ... その他のテキストスタイル
  ],
  "effects": [
    {
      "name": "Shadow/Card",
      "id": "S:effect001",
      "effects": [
        {
          "type": "DROP_SHADOW",
          "color": { "r": 0, "g": 0, "b": 0, "a": 0.1 },
          "offset": { "x": 0, "y": 4 },
          "radius": 12,
          "spread": 0,
          "visible": true,
          "blendMode": "NORMAL"
        }
      ],
      "description": "カード用の影"
    }
    // ... その他のエフェクトスタイル
  ],
  "components": [
    {
      "name": "Button/Primary",
      "id": "123:456",
      "description": "プライマリボタン",
      "type": "COMPONENT"
    }
    // ... すべてのコンポーネント（制限なし）
  ]
}
```

---

## ⚠️ 読み取れない情報

以下の情報は**保存されません**：

### レイアウト情報
- Auto Layoutの設定
- パディング、スペーシング
- 制約（Constraints）

### コンポーネントの実体
- コンポーネントの構造
- コンポーネント内のレイヤー
- バリアント設定

### グリッド・ガイド
- レイアウトグリッド
- ガイドライン

### プラグインデータ
- 他のプラグインが保存したデータ

### 変数（Variables）
- Figmaの変数機能のデータ

---

## 💡 よくある質問

### Q1: テキストスタイルで「フォントサイズだけ」読み取ることはできますか？

**A:** いいえ。テキストスタイルを適用する際は、以下の**すべての情報**が一括で適用されます：
- フォントサイズ
- フォントファミリー
- フォントスタイル（太さ）
- 行の高さ
- 文字間隔
- 大文字/小文字の設定
- 装飾（下線など）

部分的な適用はできません。

### Q2: カラースタイルで複数のグラデーションを読み取れますか？

**A:** はい。カラースタイルには以下が含まれます：
- 単色
- 線形グラデーション
- 放射状グラデーション
- 角度グラデーション
- 画像塗り

すべて正確に保存・適用されます。

### Q3: エフェクトは複数同時に適用できますか？

**A:** はい。1つのエフェクトスタイルに複数のエフェクト（例: ドロップシャドウ + インナーシャドウ）が設定されている場合、すべて適用されます。

### Q4: フォントが存在しない場合はどうなりますか？

**A:** Figmaの標準動作に従い、代替フォントで適用を試みます。エラーは発生しません。

---

## 🎯 まとめ

### 読み取れる情報の要約

| カテゴリ | 項目数 | 詳細度 |
|---------|--------|--------|
| **カラースタイル** | 無制限 | ★★★★★ 非常に詳細（グラデーション、画像塗りも含む） |
| **テキストスタイル** | 無制限 | ★★★★★ 非常に詳細（9つの属性） |
| **エフェクトスタイル** | 無制限 | ★★★★★ 非常に詳細（複数エフェクト対応） |
| **コンポーネント** | 無制限 | ★★★★★ 自動置き換え機能（バリアント保持） |

### 最適な使い方

D-kitは以下のような用途に最適です：

✅ デザインシステムのカラーパレット適用
✅ タイポグラフィの統一
✅ エフェクト（影、ブラー）の統一
✅ ブランドリニューアル
✅ 複数バリエーションの作成

---

詳細な動作確認は、実際にプラグインを使用してお試しください！
